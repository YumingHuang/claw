providers:
  default: "openai"
  list:
    - name: "openai"
      type: "openai"
      base_url: "https://api.openai.com/v1"
      api_key: "${CLAW_API_KEY}"
      model: "gpt-4"

tools:
  workdir: "${CLAW_WORKDIR}"
